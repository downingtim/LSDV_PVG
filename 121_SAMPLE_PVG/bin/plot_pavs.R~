#!/usr/bin/env Rscript

x <- read.csv("LSDV.flatten.pavs.tsv", sep="\t")
str(x)
colnames(x) <- c("val")
pdf("mfs.pdf")
hist( x$val, breaks=62, xlab=c("Number of samples"),  ylab=c("Fraction of mutations"), main="", freq=F)  
dev.off()
quit()
n